export enum LEARNING_PATH_STATUS {
  ENROLLED = 'enrolled',
  IN_PROGRESS = 'in progress',
  COMPLETE = 'completed',
}
export enum COURSE_TYPE {
  ENROLLED = 'enrolled',
  ALL = 'all',
}

export enum LEARNING_PATH_TYPE {
  ENROLLED = 'enrolled',
  ALL = 'all',
}

export enum COMPLETION_STATUS {
  NEW = 'new',
  IN_PROGRESS = 'in progress',
  COMPLETE = 'completed',
}

export enum SCENARIO_COMPLETION_STATUS {
  COMPLETE = 'complete',
  INCOMPLETE = 'incomplete',
}

export enum SCENARIO_DEPLOYMENT_STATUS {
  DEPLOYED = 'deployed',
  DEPLOYMENT_IN_PROGRESS = 'deployment in progress',
  TERMINATED = 'terminated',
  DEPLOYMENT_FAILED = 'deployment failed',
  TERMINATION_IN_PROGRESS = 'termination in progress',
  TERMINATION_FAILED = 'termination failed',
  PENDING = 'pending',
}
