export enum USER_PLAN {
  FREE_PLAN = 'free-plan',
}
export enum USER_INFO {
  TOKEN = 'token',
  X_AUTH_TOKEN = 'X-Auth-Token',
  DEFAULT = '',
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
}
export enum EMAIL_TO {
  ADMINS = 'Admins',
}
export enum EMAIL {
  SUPPORT_EMAIL = 'support@brainarator.com',
}

export enum ACTIVATION {
  ACTIVATED = 'User is already activated',
  EXPIRED = 'Activation link is expired, please try again',
}

export enum USER_ROLE {
  DEFAULT = 'Default',
  ADMIN = 'Admin',
  SUPER_USER = 'SuperUser',
}
