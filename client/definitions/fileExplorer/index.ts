export enum NODE_TYPE {
  FILE = 'file',
  FOLDER = 'folder',
}

export enum EDITOR_COMMAND {
  GET_TREE_STRUCTURE = '/opt/brainarator/dir_struct',
}

export enum URL {
  EXPLORER = '/explorer',
}

export enum SPLIT {
  PATH_SEPARATOR = '/',
}

export enum SLICE {
  START = 0,
  END = -1,
}

export enum AUTHOR_FILE_EXPLORER {
  BASE_PATH = '/home/steps/',
  ROOT_NODE = 'steps',
}

export enum TABS {
  REACT_TABS = 'react-tabs__tab',
}
export enum KEYBOARD_KEYS {
  CHAR_S_KEY_CODE = 83,
}